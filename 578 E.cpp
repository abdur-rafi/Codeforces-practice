#include <bits/stdc++.h>
using namespace std;
#define PII pair<int,int>
#define ll long long
#define loop(x,n,a) for(int x =a; x < n;++x)
#define loop_rev(x,n,a) for(int x = n-1; x >= a;--x)
char curr[1000010];
char word[1000010];
int main(){
    int n;scanf("%d",&n);
    scanf("%s",curr);
    loop(i,n,1){
        scanf("%s",word);

    }
    printf("%s",curr);
    return 0;
}
